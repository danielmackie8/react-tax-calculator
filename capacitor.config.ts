import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.danielmackie.ltdtaxcalculator',
  appName: 'IR35 Calc',
  webDir: 'build'
};

export default config;
