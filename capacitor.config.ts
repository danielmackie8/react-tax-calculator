import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.danielmackie.ltdtaxcalculator',
  appName: 'Ltd Tax Calculator',
  webDir: 'build'
};

export default config;
